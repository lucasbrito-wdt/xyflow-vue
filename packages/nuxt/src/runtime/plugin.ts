// Reserved for future Nuxt-specific runtime config (error handlers, defaults…).
// Empty plugin today; its presence means the module's runtime is wired up and
// future additions won't require a breaking change.
import { defineNuxtPlugin } from '#app';

export default defineNuxtPlugin(() => {
  // no-op
});
